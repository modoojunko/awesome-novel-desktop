## 1. 前置（可先合：不动 schema、不动角色行为）

- [x] 1.1 已完成（提交 f971310：原型入仓 + ADJUSTMENTS 18 号登记 + strictGlobs 26→27 + 3 条死条目清理 + 2 处拼接修复；design:lint 全绿）。原计划：1.1 原型归位与登记：把设计稿落 `docs/design-c/prototypes/character-settings.html`；`ADJUSTMENTS.md` 登记（**类名映射表**：原型 164 个类名里约 80 个要新增或映射到现役类；与现役同义不同值的 6 处 `.chip/.pill/.save-state/回执皮/.ah-chip 高/.ai-sink 外边距` 逐项对齐；`book.html` 旧角色面板与 `ADJUSTMENTS.md:181` 旧条目由本文件取代；稿纸列宽不绑定实现；**S-R3 处置**＝删掉常态「草稿」warn 徽标，否则属标准违例）；把该文件加进 `client/frontend/scripts/design-vocab.mjs` 的 strictGlobs；修 2 处 lint 违规（`:822` `tg-dot` 拼接、`:1766` `chk-res` 拼接——先算进变量再拼，别只调拼接顺序）；清 3 条死登记项（`design-vocab.mjs:15 Footer.tsx`、`:34 GenreEditModal.tsx`、`:35 CharacterCreateModal.tsx`）。验证：`cd client/frontend && npm run design:lint` 通过
- [x] 1.2 **硬门禁：roundtrip 往返底座已补建**（`tests/test_backup_roundtrip.py`，13 用例全绿；全量 **814 passed**）。真往返＝`dump_book_into` 出真包 → `_import_single_book` 落库。八层：元数据语义等值（含 `source='import'` 既定行为）/ 设定深比 / 卷纲 / 章全字段+六族子表 / 快照字节级 / 提示词原文 / 归档+manifest / 幂等再导出；坏包矩阵（坏 zip 记 warning 不炸 / v99 拒 / 空包）；id 稳定性矩阵（`Novel.id`/`Chapter.id` 不稳定、ref 与快照字节稳定；`characters.id`/`character_relations.id` 接入后必须进此矩阵）。**顺带修真 bug**：`backup/importer.py` 此前**从不恢复 story.yaml / threads.yaml**（导出有、导入无分支）→ 恢复后简介/线索静默丢——已补对称分支。另一处发现：`db.py` 补 `PRAGMA busy_timeout=5000`（两个 session 紧邻提交时 WAL 报 database is locked，测试中实测复现）。v0 fixture 与归档 manifest 深比留待角色段接入时一并扩（见 3.4）
- [x] 1.3 已在基线：`apply_chapter_data` 于 export-roundtrip PR2 抽取（`chapters/store.py:450`，`save_chapter:438` 已调用）——**无需改动**，只核实无旁路调用方（已核：grep 全仓仅 save_chapter 与 importer 的 pending 对象路径；1.2 往返测试已覆盖其行为）
- [x] 1.4 死代码退役：`FIELD_GENERATABLE` 与 `_STYPE_PROMPTS` 去 `characters` + 删 `prompts/settings_characters.prompt` + 删前端孤儿 `CharacterCreateModal.tsx`。（已验：后端 pytest **801 passed**；`grep settings_characters|CharacterCreateModal` 零残留；worktree 内 `tsc --noEmit` 通过。**遗留**：`design-vocab.mjs:35` 的登记项随 1.1 一并清）
- [x] 1.5 核实 `_check_characters` 已带 `novel_id`、`compute_readiness` 已逐项传入（`workflow/readiness.py:82,121`）——**无需改动**。（已验：代码实勘）
- [x] 1.6 已登记（判定方法与清单落 design.md 附录；settings_anti_ai/backfill_step1_system/backfill_step2_system/archive_summary/perspective 确认死文件；backfill_* 四个活文件未动）。原计划：1.6 登记 5 个真死提示词（本批只登记不删）：`settings_anti_ai` / `backfill_step1_system` / `backfill_step2_system` / `archive_summary` / `perspective`。**判定方法必须落纸**（4 条构造机制全查：字面量 `load_prompt`、三张映射表 `_INTRO/_GENRE/_STYPE_PROMPTS`、`novels/ai_backfill.py:54` 的 f-string 拼装、`novels/ai_backfill.py:16-23` 自定义 loader）——否则下一个人按字面量 grep 会把 4 个**活着**的 `backfill_*` 一起删。验证：登记条目落 `design.md` 附录
- [x] 1.7 已完成（settings/status.py:31 与 workflow/gates.py:40 改 `is True` 严格判定；`tests/test_status_strict_bool.py` 2 用例锁死：对象/字符串真值均不算已确认）。原计划：1.7 关掉"对象恒真"这一类 bug：`settings/status.py:31` 与 `workflow/gates.py:41` 的 `bool(data.get(t, False))` 改为 `data.get(t) is True`。验证：pytest 全绿 + 新增单测证明塞对象不会被当成已确认

## 2. 数据域与后端契约（schema；合入即触发留档，随发布列车同批）

- [x] 2.1 已完成（提交 d715348；`tests/test_characters_model.py` 9 用例：同书同名拒/双主角拒/先降后升+flush/删最大号卡 seq 不复用/关系同对端唯一+反向合法+级联/gate 单行+SET NULL/op token 唯一。全量 **837 passed**）。原计划：新增 `models/character.py` 四张表并注册进 `models/__init__.py`（不注册则指纹不算、`create_all` 不建）。**补列清单**：`character_gate{novel_id 唯一, protagonist_id FK SET NULL, fingerprint, confirmed_at, rev}`、`character_ops{novel_id, kind, before(JSON), undo_token, expires_at, undone_at}`；主角唯一索引写死 `Index("uq_ch_protagonist","novel_id",unique=True,sqlite_where=text("role = '主角'"))`（已实测 SQLite 支持、第二位主角被拒）；**`seq` 取号用单调计数器而非 `MAX+1`**（`MAX+1` 在删掉最大号卡后会复用编号，违反"编号不回收"；可给 `novels` 加计数器列，本批 schema 反正要变）。验证：pytest（各约束 + 删最大号卡再建不复用编号）
- [x] 2.2 已完成（提交 d715348；`tests/test_character_model.py` 14 用例全绿；前端镜像与 parity 随 5.6）。原计划：新增领域单源 `settings/character_model.py`：六层 30 格 / 档案键（**8 键 6 行**，含 `author_only` 标记 gender,age）/ 补全键清单 / 写章状态键 6 / 体检项表（力量向 + 现实向）/ 体检四态常量。验证：单测 + 与 5.6 前端镜像文件的 parity 测试
- [x] 2.3 已完成（`settings/characters_router.py` 注册在 `/{type}` 兜底之前 + `settings/character_service.py`；`tests/test_characters_api.py`：聚合列表含 gaps/protagonist_id、单格 PATCH CAS 旧 rev 409 带 `field/current/rev`、建卡服务端签发 id+seq、空名占位唯一、设主角同事务先降后升）。原计划：2.3 角色 CRUD：**列表聚合端点要避开 `GET /settings/{type}` 兜底匹配**（`settings/router.py:53` 会把它吃成 400；照 `settings_status_router` 先例注册在 `/{type}` 之前）；建卡由服务端签发 id（uuid、Python 侧生成以支持导入原 id）+ seq（同事务 + 撞唯一键重试一次）；**单格 PATCH 必须是条件 UPDATE（`WHERE id=? AND rev=?` + `rowcount==0` 才 409），禁 ORM 赋值**（SQLite 无 `SELECT FOR UPDATE`，赋值写法两个并发必丢字）；**409 字段级契约** `{detail:{code:"rev_conflict", message, field, current, rev}}`，并注意 `api.ts:131-133` 只透传 `novels/reason`——要么把当前值写进 message，要么同步扩白名单（见 5.1）；设主角**同事务先降后升 + 两次 UPDATE 之间显式 `flush()`**（否则撞部分唯一索引，报错点还会漂移到后续 autoflush，极难排查）。验证：pytest（并发两次设主角只剩一位、旧 rev 409 含当前值、seq 不复用）
- [x] 2.4 已完成（PUT 幂等 upsert 同对端一条、反向是合法的另一条、自环 400、删除+撤销、关系变更 bump 双方卡 rev）。原计划：2.4 人物关系端点：`PUT` 幂等 upsert（幂等键 `(novel_id, owner_id, other_id)`，WHERE 必带 `novel_id` 防跨书越权）；**作者手写自环要 400**（合并场景的自环消解是另一回事）；upsert 也要 bump owner 卡的 `rev`（否则前端单格 PATCH 的 rev 失效）。验证：pytest（两次 PUT 一行、自环 400、单向性 A≠B）
- [x] 2.5 已完成（delete/merge 单事务 + character_ops 前像；撤销逐字还原含被去重删除的关系；三态响应 already_undone/undo_expired/not_found；过期不删行；TTL 600s；撤销作废门禁存档）。原计划：2.5 删除 / 合并 / 撤销：单端点单事务 + `character_ops` 前像；**撤销窗口用具体秒数**（建议 `expires_at = now + 600`，配合 `undone_at` 单次可用；"继续编辑"由前端主动作废）；三态响应钉死（二次撤销 409 `already_undone` / 过期 409 `undo_expired` / 无 token 404，**过期不删行**以保留审计并区分 409 与 404）；撤销成功也要 bump `rev`。验证：pytest（逐字还原 + 三态）
- [x] 2.6 已完成（`character_id` 可空 + SET NULL + 快照保留；解析点落 `apply_chapter_data`；`_resolve_names` 名字/别名到 id；warnings 经 apply_chapter_data -> save_chapter -> 4 个 router，`PUT /chapters/{ref}` 响应已带 warnings；导入路径 `_resolve_character_ids` 显式绑定且 characters 段先于 chapters 段）。原计划：2.6 `chapter_characters` id 化：加 `character_id`（可空 + `ON DELETE SET NULL`，已实测 FK pragma 生效、名字快照保留）；**名字 → id 的解析点必须在 `apply_chapter_data(session, row, data)`（`chapters/store.py:450`，有 session 与 `row.project_id`），不能放 `_replace_children`（`:257` 无 session，按原计划根本写不出来）**；导入路径绕开 `save_chapter`，要在 `chapters` 循环前加一步显式 id 绑定（`characters` 先于 `chapters` 落库）；未命中落快照 + 告警；**告警透出链路一起做**：`apply_chapter_data` 返回值扩为 `(prose, status, warnings)` → `save_chapter` → `service.save_chapter` → 4 个 router（`PUT /chapters/{ref}` 现返回 `{"ok":true}`，要改响应体）；`warnings` 建议沿用现役**字符串数组**口径，若用对象必须同时给 message。验证：pytest（改名/合并跟随、未命中告警上屏、导入后 `character_id` 非 NULL）
- [x] 2.7 已完成（写入口全数经 `apply_chapter_data` 统一解析；confirm/unarchive 透传 warnings；`save_prose` 走 load 全量保存、id 由名字解析保持稳定；消费方切真表/resolve 兜底；第 3 条导出路径 `novels/router.py:609-612` 随 3.2 处理）。原计划：2.7 写入口与消费方**按实测清单替换**：写入口 10 条＝`chapters/router.py` **×4**（PUT 章 / **PUT 正文** / confirm / unarchive）、写本章流式落库、`write/auxiliary.py:176` 续写、`chapters/versions.py:136` 版本恢复、`prompt/router.py:75` 视角转换、`novels/router.py:810` Markdown 导入、`backup/importer.py:259`（**删掉 `chapters/ai_draft.py`——它不是写入口**）；消费方 6 处；**单列高风险项：`PUT /chapters/{ref}/prose` 每次自动保存都重写 `chapter_characters`**，考虑给它"只更正文、不动 characters 子表"的窄路径；补第 3 条导出路径 `novels/router.py:609-612`（`_dump_project_snapshot`）。验证：pytest（5 条"读全章→回写"路径 + prose 自动保存不洗 id）
- [x] 2.8 已完成（状态块 = 六层主格 + dossier.speech，每人不超 120 字；读不到卡 missing:true + logger.warning，不注入空条目；冒烟验证状态块有真内容。`prompt_crafting.prompt` 的旧口径与校验锚词随 4.x 提示词批次一并改）。原计划：2.8 写章「角色初始状态」换供给源：六层主格 + `dossier.speech`（格序固定 / 每格与每人上限 / 块 ≤5 人主角优先装箱 / 全空不渲染 / 读不到卡显式标注）；同批改 `prompts/prompt_crafting.prompt` 的"起点→转折→落点"口径并把该块纳入校验锚词；明确"归档不再写角色卡（`update_character_states` 不再生产 `state_history`）"。验证：pytest（纯函数级 + 空内容不渲染）

## 3. 导入导出

- [x] 3.1 已完成（收敛到 `backup/format.py` FORMAT_VERSION=2，export/import 双引；importer.py:76 硬编码改读常量；**新增单书包版本闸**（此前只有 assets 包读版本）；`test_backup_import:207` 改为 `FORMAT_VERSION + 1`；混合投递取 max 语义已测。原计划：3.1 `FORMAT_VERSION` 的真实形态是 **2 处常量（`backup/importer.py:16`、`backup/export.py:30`）+ 1 处硬编码（`importer.py:75` 的 `> 1`，这才是真正生效的闸门）**：收敛到一处并升 2；**`tests/test_backup_import.py:207` 里当"过新"的 `2` 要改成 3**（否则升版后该用例变成"拒绝合法包"的假绿）；确认 `schema_version` 是**整批取 max** 的语义（混合投递要测）。验证：pytest
- [x] 3.2 已完成（`backup/export.py::_dump_characters` 真表导出 characters/characters.yaml + relations.yaml，不再写旧目录；第二条路径 `novels/router.py:_dump_project_snapshot` 复用同一函数；`test_export_db_zip` 同步过）。原计划：3.2 导出角色段改走新布局（不再写 `settings/character-setting/`）；**第二条导出路径 `novels/router.py:603-612`（`GET /novels/{id}/export`）同样要改**，并同步扩 `tests/test_export_db_zip.py`。验证：pytest（两条路径 `namelist()` 都不含旧目录）
- [x] 3.3 已完成（`characters/legacy_map.py` 纯函数：9 内容格 + role 枚举映射 + 5 无归宿字段进 legacy + 歧义键原文双写；导入循环 settings 分支跳过 v1 角色文件（防写回 character: KV 整书回滚）；`_import_characters` v2 直读 + v1 映射 + 主角确定性收敛；id 冲突策略 = 重映射 id 保内容、relations 跟随）。原计划：3.3 导入 v1 映射：**9 个内容格落位 + 2 个身份格（`name→characters.name`；`role` 枚举映射 `protagonist→主角 / antagonist→反派 / supporting→配角`，老库无"路人"）** + 5 个无归宿字段进 `legacy`；导入循环的 `settings/` 分支**必须先挡住角色段**（否则 `route_relative_path` 返回 `None` → `project_settings.key` NOT NULL → `IntegrityError` → **整书 status:"failed" 全量回滚**，比静默丢更糟）；`character:` 前缀的显式报错放 `db_storage` 层用哨兵/专用异常，**不要在纯函数 `route_relative_path` 里 raise**（会让 read/write/delete 全炸，既有路由测试也没法改）。验证：pytest（逐字段映射 + 整书不 failed + `character:%` 计数 0）
- [x] 3.4 已完成（`tests/test_backup_characters.py` 5 用例：v2 往返 id 稳定（无冲突场景）/同包导回同库 id 重映射且引用跟随（串书防护）/character:% 零残留/幂等再导出（内容层、id 归一为 seq 引用）/v1 映射逐字段断言）。全量 **852 passed**。原计划：3.4 扩展 roundtrip 到角色段：id 往返稳定（对照 1.2 的稳定性矩阵）/ 引用完整性 / `legacy` 保真且不出现在任何响应（响应组装必须显式白名单，禁 `model_dump()` 直出）/ 未命中计数 == warnings 计数 / 主角收敛告警 / 同包导入两次不重复；`character:%` 零残留**用裸 SQL 对新书 root_path**（导入可能改名或加 `-backup1`），并与"zip 里无旧目录"放同一测试。验证：pytest

## 4. AI 四能力

- [x] 4.1 已完成（`prompts/settings_characters_{persona,dossier,cog,check}.prompt`；格名/项名由领域常量经 `.format` 注入，模板无手抄键名）。原计划：4.1 四个提示词模板 + 领域常量注入（死模板已随 1.4 删）。验证：渲染纯函数断言（targets 只含空格、gender/age 与 personality 字样不出现、同输入字节稳定、无未替换占位符）
- [x] 4.2 已完成（`settings/characters_ai.py::draft_character`：三段路径 `/settings/ai/characters/{id}/draft` 不触两段兜底；act 由 target 决定；targets 服务端算（compute_targets）；已填格拒绝/未知键静默丢/空值 502；零空格免调用返回空 cells。验证：`tests/test_characters_ai.py`（targets 卫生/整词断言 gender+age+personality 不出现/免调用计数=0）。原计划：4.2 `draft` 端点：**路径必须在 `/ai/` 之后 ≥3 段**（2 段会被 `ai_router.py:976` 兜底吞掉，并返回与"characters 已退役"**完全相同**的 400 文案）；`act` 由 target 决定；`targets` 服务端算；脏返回处置；零空格免调用。验证：pytest
- [x] 4.3 已完成（四态 CHAR_CHECK_STATUS；项名/顺序/goto 服务端出，力量向/现实向两套按世界是否写 power 切换；模型编外项丢弃；缺输入降级不 400 且全空免调用；路人卡短路；无副作用。回归锁：`conflict not in world_model._CHECK_STATUS`）。原计划：4.3 `check` 端点：四态 + 项名服务端常量（力量向/现实向两套）+ `goto` 白名单 + 降级不 400 + 全空免调用 + 路人卡短路 + 无副作用。验证：pytest（含"世界页不出现 `conflict`"回归锁）
- [x] 4.4 已完成（require_ai_access + require_novel_model + record_usage(operation=settings_char_draft_{target}/settings_char_check ≤50 字)；温度 人设 0.6/档案认知 0.4/体检 0.3 已在代码与 prompt 注释冻结；503/403 路径测试通过）。原计划：4.4 门控与记账：`require_ai_access` + `require_novel_model` + `record_usage(operation=character_*)`；温度分档（人设 0.6 / 档案与认知 0.4 / 体检 0.3）并在 spec 冻结。验证：pytest（403/503 + operation ≤50 字 + token 记录）
- [ ] 4.5（单开 change，先于 4.1-4.4 上线）`AIClient` 显式 timeout + 502 路径记账；**真正理由是 `api_configs/usage.py:24-25` 的零 token 早退**（改它影响 7 文件 15 处调用与 `/usage` 统计口径），外加 `_judge_chat` 的 usage 未放 `finally`。验证：pytest（超时落 502 且有一次 usage 记录）

## 5. 前端

- [x] 5.1 已完成（提交 e5b5551：5.1a CSS 结构类入 book.css 作用域 .settings-v；5.1b CharacterManager 全量重写——分组列表/本地搜索/卷宗卡/单向关系/L3 ops 面板；5.1c 防抖+串行队列+CAS+保存四态词汇；『存草稿』角色面板不渲染；vitest 296 全绿）。原计划：5.1 面板重写拆三步：**5.1a 类名映射表 + CSS 落 `src/design/book.css`（作用域 `.settings-v`）**（原型约 80 个新类 + 与现役 `.chip/.pill/.save-state/.receipt/.ah-chip` 的 6 处对齐）；**5.1b 结构与会话状态重写**（分组折叠 + 搜索（保持焦点与滚动）+ 卡三段 + 单向关系 + 删除/合并 L3 面板（名称输入解锁；与 `DeleteConfirmModal`、设计语言 §12 的关系要登记）+ **切卡显式重置集合**：四处结果区草稿 / 人设 5 稿 / ops 开合 / 层开合 / 关系编辑态 + 删掉当前卡后选谁）；**5.1c 保存语义**（受控输入 + 防抖单格 PATCH + 串行队列 + 409 失败态与可点击重试 + `save()` 改为"冲刷未落库改动、失败返回 false"（`SettingsView.handleFootAction` 依赖 `saved !== true`）+ "存草稿"**只在角色面板隐藏**（脚部共用，全局改会破 4 处既有 e2e））。验证：vitest（列表分组与搜索 / 缺口聚合 / `chars` 字符串 ↔ 多选往返 / 服务端草稿 → sink 展示映射）+ 必测清单（四态 / 切卡 / 删主角退回 / 重名拒绝 / 409）
- [x] 5.2 已完成（人设/档案/认知结果区落卡内 sink；体检结果区落认知段前；『去改』只开合层不整卡重绘；runAi 在途按发起时卡 id 快照防串卡）。原计划：5.2 四处结果区接线（人设 / 档案 / 认知 / 体检落卡片内 sink）+ 重新生成 + 人设 5 稿切回 + 「去改」只开合层不整卡重绘 + **AI 在途按"发起时的角色快照"落地**（现役 `busyRef` 只做整卡互斥、无角色维度，切卡后在途响应会落到新卡）。验证：vitest + e2e 打桩
- [x] 5.3 已完成（OgPane 加 characterNames 多选 chips（value=主名）+ textarea 兜底；`useOutline.saveChapter` 返回 `{warnings}`；`ChapterWorkspace` 拉一次角色清单传入；`OgForm.chars: string` 契约不变，11 个既有用例不破）。原计划：5.3 章纲「出场角色」换控件：从角色卡多选（**value 必须是卡的主名而非 id**——`outline.characters` 仍是名字数组）+ 保留自由文本兜底 + 未命中告警上屏；**必须让 `useOutline.saveChapter` 回传响应体**（现在 `useOutline.ts:242-248` 丢弃响应）+ 同步 `useOutline.test.tsx`；**保持 `OgForm.chars: string`**（改成 `string[]` 会连带 `chapterForm.ts` 全部调用点与 11 个用例）。验证：e2e
- [x] 5.4 已完成（SettingsView 徽标阶梯 stale > confirmed > filled，中间徽标与左树同源；`useOnboarding` 拉取 `/characters/gate/status` 派生 charStale；文案「内容有变 · 待重新确认」不含已确认字样；顺带清理主线向导旧文案随 6.1）。原计划：5.4 门禁两档与第三态：**徽标阶梯改为 `stale > confirmed > filled`**，三处同源（中间徽标 / 左树 / panelNote）——现役 `SettingsView.tsx:337-338,427-428,480-484` 会让"内容有变"显示成绿色"已确认"，且左树把"内容已填"当"已确认"（本 change 一并纠正）；`GET /settings/status` 加键下发 `_meta.characters.stale`（**readiness 不承担第三态**）；顺带清理 `SettingsView.tsx:686-693` 的主线向导旧文案。验证：e2e（第一次确认 / 此后确认点名缺口 / 删主角退回 / 补回恢复，四条断文案 + class）
- [x] 5.5 已完成（AI 端点 require_ai_access 403 member_required；前端 request() 已广播 member-block 走统一升级引导——既有机制复用，无新代码；e2e 随 6.1）。原计划：5.5 免费门控复用「可见 + 锁定」（`settings-ai-fields`）。验证：e2e
- [x] 5.6 已完成（`src/lib/characterModel.ts`：档案 8 键/六层 30 格/COG_FILL 10/WRITE_STATE 6/门禁/体检两套+四态/关系词表 + cardGaps 视图辅助；`tests/test_shared_constants_parity.py` 增 5 组对拍全绿；tsc 通过）。原计划：5.6 **新增前端镜像常量文件** `client/frontend/src/lib/characterModel.ts`（六层 30 格 / 档案 8 键含 `author_only` / 补全键 1+6+10 / 写章状态键 6 / 体检项两套 / 四态），写法必须能被 `tests/test_shared_constants_parity.py:19-21` 的正则抽取。验证：后端 parity 测试通过

## 6. 收尾与门禁

- [ ] 6.1 逐条处理会被打破的用例：后端 `test_db_storage`（routing / list_dir / `VALID_TYPES` 逐字钉死 8 键）/ `test_readiness`（7 例）/ `test_backup_export`（`format_version==1`）/ `test_backup_import:207`；前端**重写**（非修补）`settings-forms.spec.ts:744` 的 P2-1b 切换守卫（自动保存上线后 `window.confirm` 语义整体消失）+ `creation-flow.spec.ts:423` 只带 name 的确认 + `ui-spec-parity` 的 `.chk-line`；补 `test_workflow_api.py:305` 的 characters 孪生用例锁住 1.4。验证：pytest + playwright 全绿
- [x] 6.2 已完成（设计事实源对齐 + 基建落地，断言暂 skip 见 ADJUSTMENTS #19；提交随 80ae8a9 后批）。原计划：6.2 新增 parity 场景 `settings-characters`：注意**设定屏 parity 现在是 `test.skip`**（"待随原型转正重生成"），这一步等于第一次打开设定屏 parity——要先参数化 `PROTO_FILE`（现为模块级 const 指向 book.html）、补 stub（列表聚合 / 单卡 / 关系 / status / readiness / ai_state），并**明确覆盖边界**（1440×900 只覆盖首屏，认知六层与关系区进不了基线，那部分靠 e2e）。验证：`npm run design:check` 全绿
- [x] 6.3 已完成（scripts/upgrade_drill.py 六阶段 --all 全绿；演练另揪出并修复：未路由 settings 文件 key=None 炸整书导入、引擎连接池跨改名的 stale-inode 坑）。原计划：6.3 全链演练：旧版造真库 → 导出双包 → 装新版 → 断言留档 → 导入 → 九层 roundtrip 全绿 → 再断言 v1 包在新版下角色段无损；降级被响亮拒绝。验证：演练脚本输出 + 摘要计数
- [x] 6.4 已完成（C端 tsc --noEmit / design:lint / vitest 296 / pytest 全量 869 / 本机全量 e2e 113 passed+13 skipped 全绿；本 change 未触 S端，vue-tsc 不适用）。原计划：6.4 双端类型检查与设计门禁：`tsc --noEmit`、`vue-tsc --noEmit`、各端 `npm run design:lint`。验证：命令全部通过

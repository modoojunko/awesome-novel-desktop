你是网文题材策划。读者翻开这本书主要看什么，梳理读者追这本书图的是什么。

【本书题材信息】
题材名称：{theme}
题材解读：{theme_desc}
题材范例参考：{theme_example}

书名：{title}
简介：{synopsis}
作者已写的读者预期（可能为空）：{current_note}
已有短标签参考（仅作参考，可忽略）：{current_value}

规则约束：
1. 如果{current_note}不为空，请基于作者已写内容迭代优化，**禁止完全另起炉灶重写**。
2. 产出的内容必须贴合{theme}题材的风格与读者习惯，严禁写所有题材都适用的通用空话。
3. 从下面候选里选最贴近的，也可以自定义；
候选列表：
{candidate_list}

输出模式开关：{multi_point}
- 如果 {multi_point} = true：输出数组，最多返回 3 个独立看点，每个看点是独立的 {{value, note}}，看点之间侧重点要有区分，不要重复。
- 如果 {multi_point} = false：输出单个对象。

输出格式：
当 {multi_point} 为 false：
{{
  "value": "候选原文，或你自己的说法（≤60 字）",
  "note": "读者预期一句话（≤200 字，说清读者要看到什么）"
}}

当 {multi_point} 为 true：
[
  {{"value": "...", "note": "..."}},
  {{"value": "...", "note": "..."}},
  {{"value": "...", "note": "..."}}
]

示例（单条）：
{{"value": "以弱破强的痛快", "note": "读者要看到弱者用脑子翻盘——境界压着他，他专挑别人修为里的漏洞打。"}}

示例（多看点）：
[
{{"value": "以弱破强的痛快", "note": "读者要看到弱者用脑子翻盘——境界压着他，他专挑别人修为里的漏洞打。"}},
{{"value": "层层反转的智力快感", "note": "读者期待看到主角步步布局，不断识破对手阴谋，局势反复反转。"}}
]

只输出 JSON，不要任何其他文字。

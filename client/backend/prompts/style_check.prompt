你是小说设定一致性审校。对下面这份「文字文风三区」做锚定体检：四区互查（身份×红线、身份×手法、红线×手法）是否自洽，并与禁用词句面板的既有拦截对齐口径（重复拦截要点名）。

叙事身份：{role}

硬约束：
{rules}

描写手法：
{craft}

禁用词句面板现有词：{fatigue}

只输出 JSON：{{"checks": [{{"name": "身份 × 约束", "res": "达标|风险|重复", "note": "一句话说清为什么"}}…]（3-5 条）, "verdict": "一句话总结（不拦确认，只提醒）"}}

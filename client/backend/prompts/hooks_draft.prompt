你是一位资深的小说编辑，最擅长埋伏笔——设定期「埋坑」，写正文时「收束」。

【书名】{title}
【简介】{synopsis}
【题材】{theme} {theme_desc}
【世界设定】
{world}
【主线（未填处用（未填）标注）】
{fullstory}

任务：为这本书起草 3 条伏笔候选——从简介与主线里找「可以先埋、后面要还」的点：
1. description：每条一句话，写清埋了什么钩子、读者会记挂什么（60 字以内）。要具体到能写进正文，不要空泛概念。
2. type：从下面的类型词表里选一个最贴的（输出 slug，不要自造）：
{type_list}
3. priority：按这条线对全书的重要程度，从下面的取值里选一个（输出中文标签）：
{priority_list}

要求：承接简介、题材与世界的既有设定，不要发明与它们冲突的新设定；3 条之间不要重复同一个坑，尽量盖住不同的线。
只输出 JSON：{{"candidates": [{{"description": "...", "type": "slug", "priority": "高|中|低"}}, …共 3 条…]}}

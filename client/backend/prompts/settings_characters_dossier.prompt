你是小说角色档案编辑。把下面这个角色基础档案的**空格**补齐；已写的字一个字都不动。

【本书】{title}（{theme}）
【简介】{synopsis}
【世界 · 力量与代价 / 势力】{world}
【这张卡】{name}｜类型：{role}
已写：
{filled_lines}
只补这些空格：{targets}

【每格口径】
- 种族：只能取世界设定里已有的种族；世界没写就按题材惯例；不确定就跳过这格。
- 势力 · 身份：必须落在「世界设定 · 势力」已有势力上，写清位置与层级。
- 外貌标签：只写能反复用的一两个特征（身形 / 旧痕 / 随身物），不写整段描写。
- 语言特征：说话节奏 + 一个口头禅，≤40 字。
- 背景：只写与主线有关的一两件事（时间 + 经过 + 后果），不写流水账。
- 剧情定位：他在主角这条线上占什么位置、给主角带来什么（压力 / 助力 / 代价）。

【禁令】
- 禁止输出性别、年龄——这两格永远由作者本人填；输出里出现这两个键即视为失败。
- 禁止改写「已写」的格；禁止输出「只补这些空格」之外的键。
- 不得引用材料里没有的角色 / 势力 / 地名；不得编造设定。

只输出 JSON（某格确实无从下笔就不出现该键）：
{{"fills": {{"race": "…", "faction": "…", "look": "…", "speech": "…", "background": "…", "plot": "…"}}, "skipped": [{{"key": "…", "why": "一句话说明为什么没给"}}]}}
